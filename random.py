import random

# Function for use: This script generates a list of 18 unique random numbers between 1 and 20 using the random.sample() function.
res = random.sample(range(1, 21), 18)

print(res)
